import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  description: {
    marginBottom: 8,
    textAlign: 'center',
  },
  version: {
    textAlign: 'center',
    marginTop: 8,
    fontWeight: 'bold',
  },
});

export default styles;
