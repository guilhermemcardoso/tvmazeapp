import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    paddingVertical: 10,
  },
  listContainer: {
    padding: 16,
    borderRadius: 8,
    marginTop: 16,
  },
});

export default styles;
